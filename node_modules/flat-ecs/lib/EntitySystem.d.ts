import { EntityIteratingSystem } from './EntityIteratingSystem';
export declare abstract class EntitySystem extends EntityIteratingSystem {
    entities: number[];
    process(): void;
    processEntity(entityId: number): void;
    abstract processEntities(entities: number[]): void;
}
