import { System } from './System';
import FastBitSet from 'fastbitset';
import { World } from '.';
export declare abstract class EntityIteratingSystem extends System {
    protected componentBits: FastBitSet;
    private types;
    constructor(types: (new (...args: any[]) => any)[]);
    setWorld(world: World): void;
    process(): void;
    begin(): void;
    end(): void;
    abstract processEntity(entityId: number): void;
}
